apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  #  - ../base/linkding
  #- ../base/habitica
    #  - ../base/jellyfin
  - linkding
  - habitica
  - cloudflared
patches:
  - target:
      group: apps
      kind: Deployment
      name: linkding
    patch: |-
      - op: replace
        path: /metadata/namespace
        value: linkding


