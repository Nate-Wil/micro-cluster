apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base/linkding
    #- ../base/habitica
    # - ../base/jellyfin
patches:
  - target:
      group: apps
      kind: Deployment
      name: linkding
    patch: |-
      - op: replace
        path: /metadata/namespace
        value: linkding


